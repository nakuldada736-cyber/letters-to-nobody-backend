PORT=3000
# Comma-separated list of origins allowed to call this API.
# Use * to allow any origin (not recommended for production).
ALLOWED_ORIGINS=https://letterstonobody.net,http://localhost:5500
# Optional: where the SQLite file lives (defaults to ./data/letters.db)
DB_PATH=./data/letters.db
